# ==========================================
# FINTRAK - Variáveis de Ambiente
# ==========================================
# Copie este arquivo para .env e preencha com seus valores reais
# NUNCA commite o arquivo .env real no Git!

# ==========================================
# ⚠️ OBRIGATÓRIAS - Autenticação e Banco de Dados
# ==========================================

# URL de conexão com o banco de dados PostgreSQL (Neon)
# Exemplo para Neon: postgresql://usuario:senha@host:porta/banco?sslmode=require&channel_binding=require
# Exemplo local: postgresql://usuario@localhost:5432/motoboy_db?schema=public
DATABASE_URL="postgresql://usuario:senha@host:porta/banco?schema=public"

# Chave secreta para assinar e criptografar tokens JWT do NextAuth
# Gere uma nova chave com: openssl rand -base64 32
# IMPORTANTE: Use a mesma chave em desenvolvimento e produção
NEXTAUTH_SECRET="gere-uma-chave-aleatoria-com-openssl-rand-base64-32"

# URL base da aplicação
# Local: http://localhost:3000
# Produção: https://fintrak-omega.vercel.app (ou seu domínio customizado)
NEXTAUTH_URL="http://localhost:3000"

# ==========================================
# 🟡 OPCIONAIS - Envio de Emails (SMTP)
# ==========================================
# Configure apenas se quiser que o sistema envie emails
# (contato, pagamentos, registros com plano pago, etc.)

# Servidor SMTP
# Gmail: smtp.gmail.com
# SendGrid: smtp.sendgrid.net
# Mailgun: smtp.mailgun.org
SMTP_HOST="smtp.gmail.com"

# Porta do servidor SMTP
# Gmail: 587 (TLS) ou 465 (SSL)
# SendGrid: 587
SMTP_PORT="587"

# Usuário/email para autenticação SMTP
SMTP_USER="seu-email@gmail.com"

# Senha ou senha de app para autenticação SMTP
# Para Gmail, use uma "Senha de App" (não a senha normal):
# https://myaccount.google.com/apppasswords
SMTP_PASS="sua-senha-de-app"

# Email remetente (opcional)
# Se não configurado, usa SMTP_USER ou "noreply@fintrak.com"
SMTP_FROM="noreply@fintrak.com"

# ==========================================
# 📝 NOTAS
# ==========================================
# - NODE_ENV é definido automaticamente pelo Vercel em produção
# - Não inclua valores reais neste arquivo (apenas placeholders)
# - Para produção na Vercel, configure as mesmas variáveis em:
#   Settings → Environment Variables
